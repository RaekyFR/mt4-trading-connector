#ifndef __MARKET_ORDER_MQH__
#define __MARKET_ORDER_MQH__

void ExecuteMarketOrder(string json, string id) {
   // Extrait les données de json avec GetJsonValue / GetJsonNumber
   // Exécute un OrderSend
   // Écrit la réponse dans response.txt
}

#endif